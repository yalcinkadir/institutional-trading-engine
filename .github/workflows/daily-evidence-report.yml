name: Daily Evidence Report

on:
  workflow_dispatch:
    inputs:
      report_date:
        description: "Report date YYYY-MM-DD"
        required: true
        default: "2026-05-25"
      bootstrap_observation_only_sources:
        description: "Create observation-only Day-0 incoming seed when no real incoming sources are present"
        required: true
        default: "false"
      use_real_paper_ob