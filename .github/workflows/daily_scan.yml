name: daily_scan

# Deprecated workflow.
# Replaced by:
# .github/workflows/institutional-reports.yml
#
# Reason:
# - old single-report architecture
# - duplicate Telegram messages
# - outdated scheduling model
# - replaced by premarket / postmarket / weekly reports

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest

    steps:
      - name: Deprecated workflow notice
        run: |
          echo "daily_scan workflow is deprecated."
          echo "Use institutional-reports.yml instead."
