# Workflow Error Reports

This folder stores generated GitHub Actions workflow error reports.

The scheduled/manual workflow writes:

- `latest-workflow-errors.md`
- `YYYY-MM-DD-workflow-errors.md`

Run manually from:

`Actions → Workflow Error Report → Run workflow`
